<%= form_for(@payee) do |f| %>
<% if @payee.errors.any? %>
<div id="error_explanation">
	<h2><%= pluralize(@payee.errors.count, "error") %> prohibited this payee from being saved:</h2>

	<ul>
		<% @payee.errors.full_messages.each do |message| %>
		<li>
			<%= message %>
		</li>
		<% end %>
	</ul>
</div>
<% end %>

<table class="table table-striped">

	<tbody>
		<tr>
			<td><%= f.label :nick_name %></td>
			<td><%= f.text_field :nick_name %></td>
		</tr>
		<tr>
			<td> <%= f.label :account_name %></td>
			<td><%= f.text_field :account_name %></td>
		</tr>
		<tr>
			<td> <%= f.label :bsb %></td>
			<td> <%= f.number_field :bsb %></td>
		</tr>
		<tr>
			<td><%= f.label :account_number %></td>
			<td> <%= f.number_field :account_number %></td>
		</tr>
	</tbody>
</table>

<div class="actions">
	<%= f.submit %>
</div>
<% end %>
